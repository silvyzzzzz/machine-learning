This repository contains a Python module for scaling numerical data. 
The scaling function can be used to normalize or standardize data, which is the preprocessing step before user input goes to modeling.
